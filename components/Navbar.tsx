'use client'

import React from "react";
import { FloatingNav } from "./ui/floating-navbar";

const Navbar = () => {
  return (
    <header className="w-full mt-4 flex justify-center items-center">
      <FloatingNav
      />
    </header>
  );
};

export default Navbar;

